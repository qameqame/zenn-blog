---
title: "はじめに — このガイドについて"
---

:::message
本書のソースコードはGitHubで公開しています。
[github.com/qameqame/pgvector-tutorial](https://github.com/qameqame/pgvector-tutorial)
:::

## このガイドでやること

LLMを使ったアプリケーション開発を始めると、避けて通れない構成があります。**RAG（Retrieval-Augmented Generation）** です。

LLMは学習済みの知識しか持っていません。自社のドキュメント、最新の仕様書、プロジェクト固有の情報——こうした「モデルが知らないデータ」を扱うには、外部の知識をリアルタイムで検索して文脈に注入する仕組みが必要です。それがRAGです。

本ガイドでは、pgvectorとGeminiを使ってRAGシステムをゼロから実装し、Tool Use・AI Agents・MCPへと発展させ、最終的にクラウドにデプロイするところまで一気通貫で進めます。

---

## AI技術者の学習ロードマップと本書の位置づけ

AI技術者に必要なスキルは多岐にわたります。以下のロードマップが全体像です。

![AI Architect 学習ロードマップ](/images/ai_architect_roadmap.png)

**本書が扱う範囲は「応用」「設計」フェーズです。**

| フェーズ | 本書での扱い | 内容 |
|---------|------------|------|
| 基礎 | 前提知識として必要 | LLMの仕組み・Prompt Engineering・API/SDK |
| **応用** | ✅ **本書のメイン** | RAG・Embedding・Vector DB・Tool Use・AI Agents |
| **設計** | ✅ **本書で実装** | MCPサーバー・クラウドデプロイ（Render × Supabase） |
| 運用 | 本書の範囲外 | 評価（Evals）・Observability・セキュリティ |
| MLOps | 本書の範囲外 | CI/CD・モデル管理・デプロイ自動化 |
| 戦略 | 本書の範囲外 | ガバナンス・倫理・アーキテクチャパターン |

本書を読み終えると、「基礎」を学んだ後の最初の大きなステップである「応用〜設計フェーズ」を実装レベルで理解できます。

---

## 本書の構成

```
第1章: はじめに（本章）
第2章: RAG・Embedding・Vector DBの実装
          → pgvector環境構築・HNSWインデックス・RAGパイプライン完成
第3章: AI技術者視点で読み解くRAG設計
          → なぜpgvectorか・次元数768の理由・本番での設計判断
第4章: Tool Use実装ガイド
          → LLMが自律的にDBを検索・複数ツールの使い分け・Agenticループ
第5章: AI Agentsの実装
          → ReActパターン・メモリ機能・Plan-Execute-Evaluate
第6章: MCPサーバーの実装
          → FastMCPでサーバー化・Claude Desktop連携・HTTPモード
第7章: クラウドデプロイ
          → Render × Supabase・IPv6問題の解決・本番環境の完成
第8章: まとめと次のステップ
```

---

## 対象読者

- Pythonの実装経験があり、AIアプリ開発は初めての方
- RAG・Embedding・ベクトル検索の仕組みをコードで理解したい方
- ローカル実装からクラウドデプロイまで一気通貫で学びたい方

## 使うツール（すべて無料）

| ツール | 用途 | 無料枠 |
|--------|------|--------|
| Google Gemini API | Embeddingの生成・回答生成 | 1,500リクエスト/日 |
| pgvector（PostgreSQL拡張） | ベクトルの保存・検索 | 無制限（ローカル） |
| Docker | pgvectorの起動 | 無制限 |
| Python 3.11 | 実装言語 | — |
| Render | MCPサーバーのデプロイ | Webサービス永続無料 |
| Supabase | クラウドのpgvector | 500MB永続無料 |

---

## 3つの概念を先に整理する

### Embedding（埋め込み）とは

コンピュータは文字列のままでは「意味の近さ」を計算できません。Embeddingはテキストを数値のリスト（ベクトル）に変換する技術で、意味が近い言葉は似たパターンの数値列になります。

```
「犬」  → [0.82, 0.75, 0.10, ...]  768個の数値
「猫」  → [0.78, 0.72, 0.12, ...]  ← 犬と似たパターン
「銀行」→ [0.08, 0.10, 0.85, ...]  ← 全然違うパターン
```

この変換をGeminiのEmbeddingモデルが担当します。

### Vector DB（ベクトルデータベース）とは

通常のDBはキーワードの一致で検索しますが、Vector DBは「数値の距離」で検索します。

```sql
-- 通常の検索（キーワードが一致しないと見つからない）
SELECT * FROM docs WHERE body LIKE '%F1スコア%';

-- ベクトル検索（意味が近ければキーワードが違っても見つかる）
SELECT * FROM docs ORDER BY embedding <=> クエリのベクトル LIMIT 3;
```

「評価指標の測り方」と検索しても「F1スコアの計算方法」のドキュメントが見つかるのはこのためです。今回はPostgreSQLの拡張機能 **pgvector** を使います。

### RAG（検索拡張生成）とは

LLMは学習時のデータしか知りません。RAGは「検索して取得した情報をLLMに渡す」設計パターンで、モデルが知らないデータでも回答できるようになります。

```
【通常のLLM】  質問 → 学習済み知識のみで回答
【RAG】        質問 → Vector DBで検索 → 結果をLLMに渡す → 文書に基づいて回答
```

では、第2章の環境構築から始めましょう。
